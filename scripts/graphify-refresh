#!/usr/bin/env bash
set -euo pipefail

if [ "${1:-}" = "--help" ]; then
  exec python3 -m tools.graphify.refresh --help
fi

exec python3 -m tools.graphify.refresh --profile "${1:-coding}" --repo-root "$(pwd)"
